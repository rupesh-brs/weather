<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Weather App</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/style.css" />
</head>

<body>
    <div class="container">
        <h1 class="text-center display-5 mt-5">Weather Forecast</h1>
        <form class="d-flex justify-content-center mt-3" action="/weather" method="GET">
            <input class="form-control w-50" type="text" name="address" placeholder="Enter location" required>
            <button class="btn btn-primary ms-2" type="submit">Search</button>
        </form>

        <div class="mt-4 text-center">
            <% if (error) { %>
                <p class="text-danger"><%= error %></p>
            <% } else if (forecast) { %>
                <h4><%= location %></h4>
                <p><%= forecast %></p>
            <% } %>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
